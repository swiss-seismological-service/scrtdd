INCLUDE_DIRECTORIES(.)

SET(HDDSC_SOURCES
	utils.cpp
	ttt.cpp
	waveform.cpp
)

SET(HDDSC_HEADERS
	utils.h
	ttt.h
	waveform.h
)

SC_ADD_LIBRARY(HDDSC hddsc)

IF (HDD_INSTALL_HEADERS)
SC_LIB_INSTALL_HEADERS(HDDSC)
ENDIF()

IF (HDD_TESTS)
	SUBDIRS(test)
ENDIF()

